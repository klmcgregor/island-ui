import React from 'react';
import Button from './Button';

export default {
  title: 'Components/Button',
};

export const Default = () =>
  <Button m={[ 0, 1, 2 ]}>
    Default Button Test
  </Button>
